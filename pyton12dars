mahsulotlar = ['un', 'yog', 'kartoshka', 'piyoz', 'tuz', 'meva', 'makaron', 'gosht', 'kasha']
savat = []
mahsulot = input('mahsulot kiriring1: ')
mahsulot = input('mahsulot kiriring: ')
mahsulot = input('mahsulot kiriring: ')
mahsulot = input('mahsulot kiriring: ')
mahsulot = input('mahsulot kiriring: ')
savat.append(mahsulot)
if savat:
    for mahsulot in savat:
        if mahsulot in mahsulotlar:
           print(f"{mahsulot} bor")
        else:
            print(f'{mahsulot} mavjud emas')
